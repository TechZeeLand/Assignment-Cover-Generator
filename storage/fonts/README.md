This folder holds custom fonts uploaded through the app (persisted via a
Docker volume in production). It's intentionally empty in the repository —
uploaded `.ttf`/`.otf` files and the generated `index.json` registry live
here at runtime only.
